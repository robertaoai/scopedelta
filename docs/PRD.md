Problem: An AI tool that compares incoming feature requests against a project baseline to automatically flag scope creep and generate a formal change request.
For: Myself, acting as a strict project manager.
Core things to track: The initial baseline text, subsequent change requests, and the AI-generated variance analysis.
The one workflow (must work v1): A user submits a baseline text. Then, they submit a new request. The AI outputs a table showing if the request is in-scope or out-of-scope, and drafts a formal PMI-style change request if needed.
Success in a week: A deployed web app demonstrating the baseline-comparison logic, plus a case study detailing the AI prompt architecture.
Not v1: Multi-project management, cost calculation algorithms, user login, and PDF document parsing.

Build focus: Optimize for a polished, demoable portfolio piece a recruiter can try in 30 seconds — first impression and clarity matter.